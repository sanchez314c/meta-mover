# Changelog

See the root [CHANGELOG.md](../CHANGELOG.md) for the full version history.

This file exists so the `docs/` directory has a complete set of standard documents. All actual changelog entries live in the root `CHANGELOG.md` to follow the convention for GitHub and tooling that expects the changelog at the repository root.
